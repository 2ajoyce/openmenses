// UI entry point — Phase 1 placeholder.
// The UI layer is out of scope for Phase 1 (domain engine).
// This file satisfies the TypeScript compiler's requirement for at least one
// input file so that `tsc --noEmit` succeeds during CI.
export {};
